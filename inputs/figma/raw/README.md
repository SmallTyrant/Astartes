# inputs/figma/raw

`mcp-ingester` 또는 `scripts/fetch_figma.py`가 자동 생성하는 캐시. **수동 수정 금지.**

포맷: `{sha256[:12]}.json` — `{source_type, fetched_at, input_uri, raw, normalized}`.

수동 export는 `inputs/figma/export/`에 드롭.

상세 가이드: ../../README.md
